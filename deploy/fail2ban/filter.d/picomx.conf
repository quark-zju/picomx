# Match structured POP3 authentication failures emitted by picomx.

[Definition]
failregex = ^.*"protocol":"pop3".*"client_ip":"<HOST>".*"ban_candidate":true.*$
ignoreregex =

[Init]
journalmatch = _SYSTEMD_UNIT=picomx.service
